'''
Given an integer array nums, find the contiguous subarray w/in an array containing which has the largest product

'''


def maxProduct(nums):
    if len(nums) == 0:
        return -1

    current_max = nums[0]
    current_min = nums[0]
    final_max = nums[0]
    current_max_ = 0

    for i in range(nums):
        current_max =

        if current_max > final_max:
            fin


    pass

nums = [2,3,-2,4]